[Unit]
Description=Periodically write NTP-synced time back to the DS3231 RTC

[Timer]
# A short while after boot (let Wi-Fi/NTP settle), then hourly.
OnBootSec=90
OnUnitActiveSec=1h
Persistent=true

[Install]
WantedBy=timers.target
